import { useState, useEffect, createContext, useContext } from "react";import Navbar from "./components/Navbar";import Hero from "./components/Hero";import About from "./components/About";import Skills from "./components/Skills";import Projects from "./components/Projects";import GitHub from "./components/GitHub";import Education from "./components/Education";import Services from "./components/Services";import Resume from "./components/Resume";import Contact from "./components/Contact";import Footer from "./components/Footer";import Loader from "./components/Loader";import ScrollProgress from "./components/ScrollProgress";import CursorGlow from "./components/CursorGlow";export const ThemeContext = createContext();export const useTheme = () => useContext(ThemeContext);export default function App() {const [dark, setDark] = useState(true);const [loading, setLoading] = useState(true);useEffect(() => {const timer = setTimeout(() => setLoading(false), 2200);return () => clearTimeout(timer);}, []);const toggleTheme = () => setDark((d) => !d);if (loading) return <Loader />;return <ThemeContext.Provider value={{ dark, toggleTheme }}><div className={dark ? \"dark\" : \"\"} style={{ fontFamily: \"'Syne', sans-serif\" }}><div className=\"bg-gray-950 dark:bg-gray-950 text-white min-h-screen transition-colors duration-500\"><CursorGlow /><ScrollProgress /><Navbar /><main><Hero /><About /><Skills /><Projects /><GitHub /><Education /><Services /><Resume /><Contact /></main><Footer /></div></div></ThemeContext.Provider>;}